%h1 Kitchen Tracker

- if user_signed_in?
  - if current_user.kitchens.empty?
    .container
      %h2 Add or Create a Kitchen
      %span.buttons
        = link_to "Create", new_kitchen_path, class: "btn btn-large btn-primary"
        = link_to "Add", '#', class: 'btn btn-large btn-info'
  - else
    .container
      %ul
        - current_user.kitchens.each do |kitchen|
          %li= link_to kitchen.name, kitchen
- else
  .container
    %h2 What is this?
    .description This is a simple web application to keep track of groceries.
    %h2 Sign in or Sign Up!
    %span.buttons
      = link_to "Sign In", 'users/sign_in', class: 'btn btn-large btn-primary'
      = link_to 'Sign Up', 'users/sign_up', class: 'btn btn-large btn-info'